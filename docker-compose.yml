version: '3.8'

services:
  postgre_certification:
    container_name: postgre_certification
    image: postgres
    ports:
      -5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_certification
